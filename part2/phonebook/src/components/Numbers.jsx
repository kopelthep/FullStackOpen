//DEPRECATED, Kept for documentation.

const Numbers = ({name,number}) => {
    //console.log(name)
    return (
        <li>
            {name} : {number}
        </li>
    );
};

export default Numbers;